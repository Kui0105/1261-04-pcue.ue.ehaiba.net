# 2026-08-13 工作日志

## workbench 数据概览页改造
- 文件：`src/views/workbench/index.vue`、`src/api/app.ts`
- 去掉「版本信息」卡片（原 workbenchData.version）和「常用功能」卡片（原 workbenchData.menu）。
- 顶部新增时间范围筛选：全部 / 近7天 / 近30天 / 自定义时间段 + 重置（默认今日）。
- 数据卡片改为 4 组：访问量/总访问量、订单数/总订单数、充值金额/充值总额、退款金额/退款总额；点击卡片联动刷新（activeCard）。
- 图表区改为「订单趋势图」（折线，按日/周/月）与「订单类型趋势图」（堆叠趋势：普通/团购/秒杀，按日/周/月）；tooltip 均 trigger:'axis' 支持悬浮查看。
- `getWorkbench` 改为支持 params（range / active_card / order_granularity / order_type_granularity / start_time / end_time），后端按 `/workbench/index` 接收。
- 数据字段约定（前端 today 对象）：today_visit/total_visit、today_order/total_order、today_recharge/total_recharge、today_refund/total_refund；图表用 res.orderTrend、res.orderType。后端需补对应字段。

## Git 仓库初始化 + 首次提交
- `git init -b main`，提交首个 commit `f2e7aa4`（543 文件）。
- 远程：`https://github.com/Kui0105/1261-04-pcue.ue.ehaiba.net.git`，分支 `main`，已 push。
- 提交者：本机 git 身份 Kui0105 <951377877@qq.com>。
- GitHub 写权限凭据（PAT）已通过 Git Credential Manager 缓存，后续 push 无需再提供 token。
- 版本号：`package.json` version 由 0.0.0 → **1.0.0**。

## workbench 筛选解耦 + 卡片默认今日（v1.1.6）
- 筛选逻辑解耦：顶部时间范围（radio）只联动 4 个数据卡片；底部两张图表的右上角「按日/周/月」各自独立控制图表，固定时间窗口（按日=近30天、按周=近4周、按月=近12个月），与顶部筛选互不干扰。
- 卡片默认/重置均改为「今日」（去掉「全部」选项，因每项下方已有"总"数，语义重复）；radio 顺序：今日 / 近7天 / 近30天 / 自定义时间段；重置按钮文案「重置（今日）」。
- 订单类型趋势图系列由 普通/团购/秒杀（3个）改为 **话费充值/短信群发（2个）**：`buildStackOption()` legend+series、`fetchCharts()` 系列名循环、`src/mock/workbench.ts` 的 `orderType.list` 三维→二维 全部同步。
- 自定义时间段 date-picker 宽度用内联 `style="width: 240px"` 固定（Tailwind 类被 Element Plus inline-flex 覆盖无效）。
- 版本号：1.1.5 → **1.1.6**；commit `d1a1eac` 已 push origin main。
- mock 开关用 `VITE_USE_MOCK = 'true'`（`.env.development`），而非 `import.meta.env.DEV`，因该文件 NODE_ENV=production 使 DEV 恒 false。
- 注：vue-tsc --noEmit 报大量历史错误（JSX IntrinsicElements / $router / $route 等），均与本次改动无关，未处理。

## 用户管理页改版（按需求文档 v1.1.7）
- 文件：`src/api/consumer.ts`、`src/views/consumer/lists/index.vue`、`src/views/consumer/lists/detail.vue`，以及新建 5 个组件：
  - `components/user-recharge.vue`（余额充值弹窗：金额 + 当前管理手机号 + 验证码 + 60s 倒计时）
  - `components/user-credit.vue`（修改预授信额度弹窗）
  - `components/user-parent.vue`（修改上级弹窗：远程搜索用户）
  - `components/user-orders.vue`（订单列表 TAB 内嵌）
  - `components/user-transactions.vue`（交易记录 TAB 内嵌）
- 列表筛选项：用户信息（ID/昵称/手机号）、用户类型（个人/企业）、注册时间段；保留 export-data 导出按钮（按筛选结果导出）。
- 列表列：用户ID / 头像 / 昵称 / 手机号 / 用户类型 / 上级用户（昵称+ID）/ 余额 / 用户状态（el-switch 启用禁用切换）/ 注册时间；操作列：详情、余额充值、修改额度、修改上级。
- 详情页：基本信息（ID/头像/昵称/手机号/类型/上级/状态/注册时间）+ 账户信息卡（余额 / 预授信额度 / 累计充值 / 累计消费）+ 订单列表 TAB + 交易记录 TAB。
- API 新增：`toggleUserStatus`、`rechargeUser`、`sendRechargeCode`、`updateCreditLimit`、`updateParentUser`、`searchUser`、`getUserOrders`、`getUserTransactions`。
- 关键约定：所有"修改"操作（充值、额度、上级）均先 feedback.confirm 二次确认再提交；状态切换走 `toggleUserStatus`；recharge 流程含验证码（60s 倒计时）。
- 版本号：1.1.6 → **1.1.7**；commit `d98dfee` 已 push origin main。

## 用户管理个人用户余额功能调整（v1.2.0）
- 文件：`src/views/consumer/lists/index.vue`、`src/views/consumer/lists/detail.vue`、`package.json`。
- 列表余额列：个人用户（user_type == 1）显示 `-`，企业用户显示 `¥ xxx`。
- 列表操作列：个人用户隐藏「余额充值」「修改额度」按钮，保留「详情」「修改上级」。
- 详情页：个人用户移除「账户信息」卡片；将「累计消费金额」移入「基本信息」。
- 版本号：1.1.7 → **1.2.0**；commit `8b3cf24` 已 push origin main。

## 新增订单管理与订单详情页面（v1.3.0）
- 新增文件：
  - `src/views/consumer/order/index.vue`（订单列表页）
  - `src/views/consumer/order/detail.vue`（订单详情页，兼容话费充值 / 短信群发两种类型）
  - `src/api/order.ts`（订单 API 层，含 mock 开关）
  - `src/mock/order.ts`（本地 mock 数据）
  - `src/config/staticMenus.ts`（静态菜单配置：订单管理 → 订单列表/详情）
- 修改文件：`src/stores/modules/user.ts`（登录后自动将静态菜单合并到「用户管理」目录下，避免后端未配置时菜单缺失）。
- 订单列表：顶部筛选（用户信息/订单编号/订单类型/订单状态/税费类型/提交时间）、导出、统计卡片（订单总数/进行中/已完成）、列表列（订单编号/类型/税费类型/成功失败总数/退款金额/支付总额/状态/提交时间）、查看详情操作。
- 订单详情：基本信息（按类型显示运营商/面额/短信模板）、统计卡片（号码总数/进行中/成功/失败/退款总额）、明细列表（手机号/状态/结果说明/回调时间）含筛选导出。
- 分页默认 20 条/页，mock 数据 43 条；开发环境下 `VITE_USE_MOCK=true` 可直接预览。
- 版本号：1.2.0 → **1.3.0**；commit `f095516` 已 push origin main。

## 订单管理功能细化（v1.3.2）
- 订单列表 `index.vue` 与详情 `detail.vue` 均新增「下单用户」字段（用户昵称 / 手机号）；列表新增对应列，详情基本信息新增该条目。
- 订单状态精简为 **进行中 / 已完成** 两个状态（移除已失败）：`mock/order.ts` 的 `ORDER_STATUS_TEXT` 与列表筛选下拉同步删除已失败选项。
- 号码明细状态随订单状态联动（核心逻辑）：
  - 话费充值：充值中 / 充值成功 / 充值失败；订单「进行中」时三种均存在，「已完成」时仅充值成功 / 充值失败。
  - 短信群发：发送中 / 发送成功 / 发送失败；同上。
  - 实现：`mock/order.ts` 的 `buildItems(base)` 按 `base.order_status` 用确定性伪随机（seed=base.id）生成明细，保证详情统计卡片与明细列表一致；订单对象在 `genOrders` 时预生成 `items`。
- 订单详情基本信息由左侧单列 `el-form` 改为 `el-descriptions :column="3"` 三列网格，解决右侧空旷问题；短信模板条目 `:span="3"` 占满整行。
- 详情统计卡片的 pending/success/fail 数量改为基于预生成明细实时统计（`getOrderDetailMock` 内计算），与列表完全一致。
- 版本号：1.3.1 → **1.3.2**；commit `c2b7f10` 已 push origin main。

## 新增方案管理-话费充值档位与短信模板（v1.4.0）
- 静态菜单：`src/config/staticMenus.ts` 改造为 `{ after, menu }` 结构，`src/stores/modules/user.ts` 的 `mergeStaticMenus` 按 `after` 字段顺序注入；新增「方案管理」目录位于「订单管理」之后、「应用管理」之前，子菜单：话费充值、短信模板。
- 新增文件：
  - `src/views/plan/recharge/index.vue`（话费充值档位页）
  - `src/views/plan/sms/index.vue`（短信模板页）
  - `src/api/plan.ts`（方案管理 API 层，含 mock 开关）
  - `src/mock/plan.ts`（话费充值 / 短信模板 mock 数据与 CRUD）
- 话费充值：列表展示充值面额、单价、用户类型、排序、操作；新增/编辑弹窗含充值面额、单价、税费类型、用户类型、排序；删除二次确认；单价选择含税自动按 1.06 倍计算展示；mock 中限制每个用户类型最多 6 个档位；分页 20 条/页。
- 短信模板：顶部筛选（用户昵称/手机号、签名名称、创建时间）；列表展示模板ID、关联用户、短信签名、模板内容、单价、创建时间；新增/编辑含模板ID、关联用户（远程搜索）、签名名称、模板内容、单价、税费类型；删除二次确认；单价含税按 1.06 倍计算；分页 20 条/页，按时间倒序。
- 版本号：1.3.2 → **1.4.0**；commit `97b2dd7` 已 push origin main。

## 方案管理-去除税费类型（v1.4.1）
- 文件：`src/views/plan/recharge/index.vue`、`src/views/plan/sms/index.vue`、`src/mock/plan.ts`、`package.json`。
- 话费充值 / 短信模板 新增编辑弹窗：移除「税费类型」字段（原含税率选择），含税比例写死为 1.06，不再可配置。
- 列表「单价」列移除「含税」标签；详情/弹窗的「含税单价」改为始终展示（= 单价 × 1.06）。
- 短信模板「关联用户」保持为可输入搜索的下拉（filterable + remote，`searchUser` 按昵称/手机号远程搜索），本次未改动该行为。
- `mock/plan.ts`：`calcPrice` 改为始终按 1.06 倍计算（忽略 taxType），所有新增/编辑记录强制 `tax_type = 1`，保证未发送 tax_type 时价格仍含税。
- 版本号：1.4.0 → **1.4.1**；commit `1a6db96` 已 push origin main。

## 新增财务管理模块（v1.5.0）
- 静态菜单：`src/config/staticMenus.ts` 新增「财务管理」目录（paths: finance）位于「方案管理」之后；`src/stores/modules/user.ts` 的 `mergeStaticMenus` 改为：若后端已存在同 paths 目录，先移除原位置再按 `after` 重新插入，实现目录位置调整与子菜单替换。
- 清理旧财务页面：删除 `src/views/finance/balance_details.vue`、`recharge_record.vue`、`refund_record.vue` 及 `component/refund-log.vue`。
- 新增文件：
  - `src/api/finance.ts`（交易明细 / 佣金记录 / 提现申请 / 提现设置 API 层，含 mock 开关）
  - `src/mock/finance.ts`（以上 4 个模块的 mock 数据与状态变更）
  - `src/views/finance/transaction/index.vue`：筛选（用户、签名、时间）+ 4 统计卡片 + 列表（流水号/订单/用户/类型/金额/余额/账户/时间）
  - `src/views/finance/commission/index.vue`：筛选（订单号/用户/代理商/结算时间）+ 4 统计卡片 + 列表（订单/用户/类型/实付/代理商/层级/返佣/时间）
  - `src/views/finance/withdraw/index.vue`：筛选（代理商/时间）+ TAB（待审核/待打款/已完成/审核驳回）+ 列表（单号/代理商/金额/手续费/打款/账户/状态/时间）+ 操作（审核通过、驳回、确认打款、查看凭证）
  - `src/views/finance/setting/index.vue`：最低/单笔/单日最高提现额度、提现手续费（0-100%）表单与保存
- 分页均默认 20 条/页，按时间倒序；mock 数据开发环境可直接预览。
- 版本号：1.4.1 → **1.5.0**；commit `5274fb8` 已 push origin main。

## 财务管理页面优化调整（v1.5.1）
- 文件：`src/views/finance/transaction/index.vue`、`src/views/finance/commission/index.vue`、`src/views/finance/withdraw/index.vue`、`src/views/finance/setting/index.vue`、`src/mock/finance.ts`、`package.json`。
- 交易明细（change 1/2/3）：
  - 顶部筛选「签名名称」改为「交易类型」下拉（话费充值/短信群发/平台充值/平台扣减/订单退款），并新增「交易账户」下拉（系统余额/微信支付）。
  - 列表「交易金额」去掉 +/- 加减号，仅显示 ¥ 金额（保留颜色区分）。
  - 交易账户映射（mock 同步）：微信支付仅 话费充值/短信群发/订单退款；系统余额支持全部五种类型。
  - 订单退款（type 5）记录补充「关联订单」数据；列表「关联订单」列对无订单类型显示 `--`。
- 佣金记录（change 4/5）：
  - 统计卡片 累计佣金/累计提现/提现中/待提现 统一 `toFixed(2)` 保留两位小数（新增 `formatMoney`）。
  - 分销层级由 1/2/3 改为仅 一级/二级（`distribution_level_text` 字段），列表列改用该文本。
- 提现申请（change 6）：TAB 新增「全部」（name="" 展示所有状态，默认激活）；「审核驳回」TAB 下列表新增「驳回原因」列（v-if status==='3'），展示 `reject_reason`。
- 提现设置（change 7）：表单样式重构——新增标题与说明、标签加宽至 190px、数字输入框固定 220px 宽（解决原 `w-full` 过度拉伸）、每字段增加辅助说明、保存按钮文案改为「保存设置」。
- 版本号：1.5.0 → **1.5.1**；commit `95e006e` 已 push origin main。

## 订单号蓝色可点击跳转订单详情（v1.5.2）
- 4 处订单号改为蓝色（`text-primary`）+ 可点击（`cursor-pointer hover:underline`）：
  - `src/views/consumer/order/index.vue` 订单列表「订单编号」列 → `goDetail(row)`（已有 router）
  - `src/views/consumer/components/user-orders.vue` 用户详情订单列表「订单号」列 → 新增 router + `goDetail`
  - `src/views/finance/transaction/index.vue` 交易明细「关联订单」列 → 新增 router + `goDetail`（无订单时显示 `--`）
  - `src/views/finance/commission/index.vue` 佣金记录「关联订单」列 → 新增 router + `goDetail`（无订单时显示 `--`）
- 跳转统一 `router.push({ path: '/order/detail', query: { id: row.id, order_sn: row.order_sn } })`。
- 详情页 `src/views/consumer/order/detail.vue` 与 `src/mock/order.ts` 的 `getOrderDetailMock` 增加 `order_sn` 兜底查询：优先按 id，其次按 order_sn，最后回退首条；因用户订单/交易/佣金 mock 行仅含 order_sn（且格式与订单 mock 不同），mock 下会落到首条订单，预览可用；正式后端按 id/order_sn 解析。
- 版本号：1.5.1 → **1.5.2**；commit `f6ef84e` 已 push origin main。

## 新增代理商管理模块（v1.6.0）
- 静态菜单：`src/config/staticMenus.ts` 新增「代理商管理」目录（paths: agent，icon: `el-icon-OfficeBuilding`）位于「财务管理」之后；子菜单：代理商列表、代理商申请。
- 新增文件：
  - `src/api/agent.ts`（代理商列表/状态切换/详情/佣金明细、申请列表/审核通过/审核驳回）
  - `src/mock/agent.ts`（40 个代理商 + 36 条申请 + 佣金明细 mock 与 CRUD）
  - `src/views/agent/list/index.vue`：筛选（代理商昵称/手机号、状态、创建时间）+ 导出 + 列表（用户ID/昵称手机号/类型/邮箱/营业执照/佣金账户/状态/创建时间/操作）；状态用 `el-switch` 启用/禁用；「查看详情」弹窗含基本信息/账户信息/佣金明细 TAB。
  - `src/views/agent/apply/index.vue`：筛选（用户昵称/手机号、提交时间）+ TAB（全部/待审核/审核通过/审核驳回）+ 列表（用户ID/手机号/类型/邮箱/营业执照/提交时间/状态）+ 操作（审核通过、审核驳回）；驳回弹窗填写原因。
- 分页默认 20 条/页，按时间倒序；mock 数据开发环境可直接预览。
- 版本号：1.5.2 → **1.6.0**；commit `8623c5f` 已 push origin main。

## 代理商管理页面优化（v1.6.1）
- 文件：`src/views/agent/list/index.vue`、`src/views/agent/apply/index.vue`、`src/mock/agent.ts`、`package.json`。
- 代理商列表：
  - 列表列「代理昵称/手机号」改为 **真实姓名**（`real_name`）+ **联系电话**（`contact_phone`）两列（mock 新增 `genRealName` 生成中文姓名、`contact_phone` 字段）。
  - 状态列移除「启用/禁用」文字，仅保留 `el-switch` 开关。
  - 列表列「佣金账户」→ **佣金余额**（显示 `commission_balance`）。
  - 列表新增 **累计佣金**（`total_commission`）、**已提现佣金**（`withdrawn_commission`）两列。
  - 详情弹窗「佣金明细」TAB 分页由 20 改为 **10** 条/页（`commissionSize` 默认 10）。
  - 筛选占位符改为「真实姓名/联系电话」，关键词搜索覆盖 `real_name`/`contact_phone`。
- 代理商申请：列表「手机号码」列改为 **真实姓名** + **联系电话** 两列；筛选占位符同步为「真实姓名/联系电话」，关键词搜索覆盖新字段。
- 版本号：1.6.0 → **1.6.1**；commit `b7413ec` 已 push origin main。
