# 2026-08-13 工作日志

## workbench 数据概览页改造
- 文件：`src/views/workbench/index.vue`、`src/api/app.ts`
- 去掉「版本信息」卡片（原 workbenchData.version）和「常用功能」卡片（原 workbenchData.menu）。
- 顶部新增时间范围筛选：全部 / 近7天 / 近30天 / 自定义时间段 + 重置（默认今日）。
- 数据卡片改为 4 组：访问量/总访问量、订单数/总订单数、充值金额/充值总额、退款金额/退款总额；点击卡片联动刷新（activeCard）。
- 图表区改为「订单趋势图」（折线，按日/周/月）与「订单类型趋势图」（堆叠趋势：普通/团购/秒杀，按日/周/月）；tooltip 均 trigger:'axis' 支持悬浮查看。
- `getWorkbench` 改为支持 params（range / active_card / order_granularity / order_type_granularity / start_time / end_time），后端按 `/workbench/index` 接收。
- 数据字段约定（前端 today 对象）：today_visit/total_visit、today_order/total_order、today_recharge/total_recharge、today_refund/total_refund；图表用 res.orderTrend、res.orderType。后端需补对应字段。

## Git 仓库初始化 + 首次提交
- `git init -b main`，提交首个 commit `f2e7aa4`（543 文件）。
- 远程：`https://github.com/Kui0105/1261-04-pcue.ue.ehaiba.net.git`，分支 `main`，已 push。
- 提交者：本机 git 身份 Kui0105 <951377877@qq.com>。
- GitHub 写权限凭据（PAT）已通过 Git Credential Manager 缓存，后续 push 无需再提供 token。
- 版本号：`package.json` version 由 0.0.0 → **1.0.0**。

## workbench 筛选解耦 + 卡片默认今日（v1.1.6）
- 筛选逻辑解耦：顶部时间范围（radio）只联动 4 个数据卡片；底部两张图表的右上角「按日/周/月」各自独立控制图表，固定时间窗口（按日=近30天、按周=近4周、按月=近12个月），与顶部筛选互不干扰。
- 卡片默认/重置均改为「今日」（去掉「全部」选项，因每项下方已有"总"数，语义重复）；radio 顺序：今日 / 近7天 / 近30天 / 自定义时间段；重置按钮文案「重置（今日）」。
- 订单类型趋势图系列由 普通/团购/秒杀（3个）改为 **话费充值/短信群发（2个）**：`buildStackOption()` legend+series、`fetchCharts()` 系列名循环、`src/mock/workbench.ts` 的 `orderType.list` 三维→二维 全部同步。
- 自定义时间段 date-picker 宽度用内联 `style="width: 240px"` 固定（Tailwind 类被 Element Plus inline-flex 覆盖无效）。
- 版本号：1.1.5 → **1.1.6**；commit `d1a1eac` 已 push origin main。
- mock 开关用 `VITE_USE_MOCK = 'true'`（`.env.development`），而非 `import.meta.env.DEV`，因该文件 NODE_ENV=production 使 DEV 恒 false。
- 注：vue-tsc --noEmit 报大量历史错误（JSX IntrinsicElements / $router / $route 等），均与本次改动无关，未处理。

## 用户管理页改版（按需求文档 v1.1.7）
- 文件：`src/api/consumer.ts`、`src/views/consumer/lists/index.vue`、`src/views/consumer/lists/detail.vue`，以及新建 5 个组件：
  - `components/user-recharge.vue`（余额充值弹窗：金额 + 当前管理手机号 + 验证码 + 60s 倒计时）
  - `components/user-credit.vue`（修改预授信额度弹窗）
  - `components/user-parent.vue`（修改上级弹窗：远程搜索用户）
  - `components/user-orders.vue`（订单列表 TAB 内嵌）
  - `components/user-transactions.vue`（交易记录 TAB 内嵌）
- 列表筛选项：用户信息（ID/昵称/手机号）、用户类型（个人/企业）、注册时间段；保留 export-data 导出按钮（按筛选结果导出）。
- 列表列：用户ID / 头像 / 昵称 / 手机号 / 用户类型 / 上级用户（昵称+ID）/ 余额 / 用户状态（el-switch 启用禁用切换）/ 注册时间；操作列：详情、余额充值、修改额度、修改上级。
- 详情页：基本信息（ID/头像/昵称/手机号/类型/上级/状态/注册时间）+ 账户信息卡（余额 / 预授信额度 / 累计充值 / 累计消费）+ 订单列表 TAB + 交易记录 TAB。
- API 新增：`toggleUserStatus`、`rechargeUser`、`sendRechargeCode`、`updateCreditLimit`、`updateParentUser`、`searchUser`、`getUserOrders`、`getUserTransactions`。
- 关键约定：所有"修改"操作（充值、额度、上级）均先 feedback.confirm 二次确认再提交；状态切换走 `toggleUserStatus`；recharge 流程含验证码（60s 倒计时）。
- 版本号：1.1.6 → **1.1.7**；commit `d98dfee` 已 push origin main。

## 用户管理个人用户余额功能调整（v1.2.0）
- 文件：`src/views/consumer/lists/index.vue`、`src/views/consumer/lists/detail.vue`、`package.json`。
- 列表余额列：个人用户（user_type == 1）显示 `-`，企业用户显示 `¥ xxx`。
- 列表操作列：个人用户隐藏「余额充值」「修改额度」按钮，保留「详情」「修改上级」。
- 详情页：个人用户移除「账户信息」卡片；将「累计消费金额」移入「基本信息」。
- 版本号：1.1.7 → **1.2.0**；commit `8b3cf24` 已 push origin main。

## 新增订单管理与订单详情页面（v1.3.0）
- 新增文件：
  - `src/views/consumer/order/index.vue`（订单列表页）
  - `src/views/consumer/order/detail.vue`（订单详情页，兼容话费充值 / 短信群发两种类型）
  - `src/api/order.ts`（订单 API 层，含 mock 开关）
  - `src/mock/order.ts`（本地 mock 数据）
  - `src/config/staticMenus.ts`（静态菜单配置：订单管理 → 订单列表/详情）
- 修改文件：`src/stores/modules/user.ts`（登录后自动将静态菜单合并到「用户管理」目录下，避免后端未配置时菜单缺失）。
- 订单列表：顶部筛选（用户信息/订单编号/订单类型/订单状态/税费类型/提交时间）、导出、统计卡片（订单总数/进行中/已完成）、列表列（订单编号/类型/税费类型/成功失败总数/退款金额/支付总额/状态/提交时间）、查看详情操作。
- 订单详情：基本信息（按类型显示运营商/面额/短信模板）、统计卡片（号码总数/进行中/成功/失败/退款总额）、明细列表（手机号/状态/结果说明/回调时间）含筛选导出。
- 分页默认 20 条/页，mock 数据 43 条；开发环境下 `VITE_USE_MOCK=true` 可直接预览。
- 版本号：1.2.0 → **1.3.0**；commit `f095516` 已 push origin main。
