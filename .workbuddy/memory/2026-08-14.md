# 2026-08-14 工作日志

- 去掉 ADMIN 接口访问前缀：将 `src/config/index.ts` 的 `urlPrefix: 'adminapi'` 改为 `''`；同步修正 `src/components/upload/index.vue` 上传地址拼接（前缀为空时用空串、避免 `BASE_URL//upload` 双斜杠）。所有 `/auth.admin/...`、`/setting.*` 等真实接口请求路径由 `BASE_URL/adminapi/...` 变为 `BASE_URL/...`。
- 版本号：1.6.5 → **1.6.6**；commit `8db9a7e` 已 push origin main。
- 注意：去掉前缀后端需相应在根路径提供接口；axios combineURLs 已自动处理 baseURL 末尾 `/` 与 url 开头 `/` 的合并，无双斜杠问题。
