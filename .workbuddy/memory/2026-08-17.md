# 2026-08-17 工作日志

- 新增「客户管理」模块：
  - `src/config/staticMenus.ts`：在「代理商管理」下方新增「客户管理」目录（paths: customer，icon: el-icon-UserFilled），子菜单「客户绑定申请」（paths: binding，component: customer/binding/index，perms: customer.bind/lists）。
  - `src/api/customer.ts`：新增 getCustomerBindList / submitCustomerBind / approveCustomerBind / rejectCustomerBind，均带 VITE_USE_MOCK 开关，真实接口路径前缀 `/customer.bind/*`。
  - `src/mock/customer.ts`：客户绑定申请 mock 数据（35 条），含 id/user_id/昵称/手机/姓名/用户类型/成交图片/状态/驳回原因/提交时间；支持关键字、状态、时间段筛选；按时间倒序；submit/approve/reject 会修改内存数据。
  - `src/views/customer/binding/index.vue`：客户绑定申请页面，含顶部筛选（用户昵称/手机/姓名、申请时间段）、TAB（全部/待审核/审核通过/审核驳回）、表格列（用户ID/手机号码/用户类型/成交图片/状态/提交时间/驳回原因）、操作（新增申请/审核通过/审核驳回/编辑）；新增/编辑弹窗支持远程搜索用户（按手机/昵称/ID）、自动回填手机与用户类型、上传最多3张成交图片；驳回弹窗填写原因。分页 20 条/页。
- 版本号：1.6.12 → **1.7.0**；commit `34ed630` 已 push origin main。
- 注：vue-tsc --noEmit 报大量历史错误（JSX IntrinsicElements / $router / $route 等），均与本次改动无关，未处理。
