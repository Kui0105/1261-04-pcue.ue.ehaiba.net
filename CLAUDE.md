# 通用项目开发规范

## 1. 适用范围

本文件用于约束后端、管理后台、Web/H5、小程序、App 及 UniApp 等项目的日常开发。

执行时遵循以下优先级：

1. 当前任务已明确的需求、接口协议和验收标准。
2. 项目现有架构、目录结构、命名方式和代码风格。
3. 本文件中的通用规范。

二次开发必须保持与现有框架一致，不得为了套用规范而重构无关的稳定代码。

## 2. 开发原则

- 仅修改完成当前任务所必需的代码，不做无关重构、大规模格式化或目录调整。
- 新增功能前，先在相关业务范围内查找可复用的组件、Model、Service、事件、hooks、utils、stores 和接口封装。
- 搜索应限定范围，并排除依赖目录、构建产物、缓存、日志和上传文件，避免无效结果。
- 优先使用项目已有能力；非必要不新增依赖、基础设施或重复封装。
- 修改公共逻辑前，先确认调用方、事件监听、缓存、异步任务及其他下游影响。
- 技术方案必须兼顾正常流程、异常参数、权限、并发、重复请求和失败恢复。
- 不确定的业务规则不得自行推断或写死，应先确认现有实现、接口约定或需求说明。

## 3. 开发流程

### 3.1 开发前

- 阅读目标模块及其直接关联代码，明确数据来源、调用链、状态变化和边界条件。
- 确认是否存在可复用实现，避免创建功能相同但行为不一致的代码。
- 涉及接口调整时，先明确请求参数、响应结构、状态枚举、错误码和兼容策略。
- 涉及数据库变更时，确认实际表结构、字段含义、索引、历史数据和回滚影响。
- 涉及多个端时，明确各端职责，服务端负责最终校验和数据一致性，客户端负责交互反馈和基础校验。

### 3.2 开发中

- 保持修改范围集中，避免将功能变更与代码整理混在同一次修改中。
- 沿用现有分层和数据访问方式，不绕过 Model、Repository、Service 等项目约定的业务层直接操作数据。
- 公共方法应保持职责单一，复杂分支应拆分为可读、可测试的逻辑单元。
- 变量、方法、类和文件命名应准确表达用途，避免无意义缩写、拼音和临时名称。
- 注释用于解释业务原因、边界条件和非显而易见的决策，不重复描述代码本身。
- 禁止使用空的异常捕获隐藏错误；异常应按项目约定记录、转换或继续抛出。
- 配置项、限制值和环境差异应进入统一配置，不得散落硬编码。
- 不得在代码、日志、注释、测试数据或提交内容中写入密钥、Token、密码、证书和私有地址。

### 3.3 开发后

- 执行与修改范围匹配的最小验证，例如语法检查、单元测试、lint、type-check 或目标端局部构建。
- 至少验证正常流程、空值、格式错误、边界值、权限不足、重复操作和失败分支。
- 涉及公共方法、数据结构或跨模块契约时，应补充或更新对应测试。
- 检查是否引入调试输出、临时代码、未使用依赖、无效注释或敏感信息。
- 交付时说明修改范围、验证结果、兼容性影响和未覆盖风险。

## 4. 后端开发规范

### 4.1 接口设计

- 接口输入和输出应保持稳定、结构清晰，字段命名、数据类型和空值语义全项目一致。
- 新增或修改接口时，应明确必填项、默认值、枚举范围、分页规则、排序规则和错误响应。
- 不直接向客户端暴露内部异常、SQL、文件路径、堆栈或第三方原始错误。
- 接口变更应优先保持向后兼容；无法兼容时，应明确版本或迁移方案。
- 列表、详情、导出等不同入口应使用一致的数据权限、筛选规则和字段口径。

### 4.2 参数与权限校验

- 所有外部输入必须在服务端校验，不能仅依赖前端校验。
- 校验范围包括类型、格式、长度、范围、枚举、文件、资源归属和业务状态。
- 鉴权成功不等于有权操作目标资源；每个受保护操作都必须校验角色、权限和数据归属。
- 批量操作必须逐项或按可靠条件校验权限，不能只校验其中一个对象。
- 客户端传入的价格、金额、折扣、权限、状态等关键值不得直接作为可信结果使用。

### 4.3 数据与并发

- 数据读写应通过项目统一的数据访问层完成，保证缓存、事件、审计和业务规则能够正常生效。
- 多表写入、状态流转和资金类操作应使用事务，并正确处理异常回滚。
- 唯一性不能只通过“先查询再写入”保证，应结合唯一索引、原子更新或锁机制。
- 重复请求可能造成副作用时，应通过幂等键、唯一约束或业务状态机保证幂等。
- 高并发操作应根据场景使用条件更新、数据库锁或分布式锁，并处理锁超时和异常释放。
- 修改缓存相关数据时，应使用项目统一的缓存更新或失效机制，避免数据库与缓存不一致。

### 4.4 文件上传

- 上传文件必须校验文件大小、扩展名、MIME 类型和实际文件内容，不能只信任客户端文件名。
- 文件大小、类型和尺寸限制应由配置或统一常量管理，并与前端提示保持一致。
- 上传文件应使用安全文件名和受控存储路径，禁止用户输入直接参与路径拼接。
- 非公开文件不得存放在可直接访问的公共目录；下载时必须执行权限校验。
- 图片处理应考虑压缩、方向、尺寸、异常文件和处理失败场景。

### 4.5 日志与异常

- 日志应包含定位问题所需的请求标识、业务标识、操作结果和必要上下文。
- 禁止记录密码、完整 Token、验证码、密钥及未脱敏的敏感信息。
- 日志级别应与事件严重程度匹配，预期内的业务校验失败不应记录为系统异常。
- 对外返回统一、可理解的错误信息；对内保留足够的排查上下文。

## 5. 管理后台与 Web/H5 规范

- 优先复用项目已有布局、表单、表格、弹窗、上传和反馈组件。
- 表单必填项应有明确标识，并同时实现前端提示和服务端校验。
- 邮箱、手机号、身份证号、银行卡号等固定格式字段应进行格式校验，但前端校验不能替代服务端校验。
- 密码输入默认使用密码类型，不得以普通文本框展示密码。
- 提交期间应禁用重复触发，并提供明确的加载、成功和失败反馈。
- 列表和详情页面应完整处理加载、空数据、错误、无权限和数据失效状态。
- 图片上传前按项目要求压缩和校验，上传控件应展示支持格式、大小或推荐尺寸。
- 页面不得展示未经授权的敏感数据；复制、导出、日志和错误提示同样遵守脱敏规则。
- 网络请求、鉴权、路由、存储和错误处理应使用项目统一封装。
- 样式修改应兼顾常用分辨率，避免文字溢出、控件重叠和仅在单一尺寸下可用。

## 6. UniApp 跨端规范

以下规则对 UniApp 跨端代码强制执行：

1. 尺寸优先使用 `rpx`，仅在明确需要固定物理尺寸时使用 `px`。
2. 不直接依赖 `window`、`document` 等浏览器 API；H5 专属代码放入 `#ifdef H5`。
3. 小程序通用代码放入 `#ifdef MP`，微信小程序专属代码放入 `#ifdef MP-WEIXIN`。
4. 避免复杂选择器、不稳定伪类等小程序兼容性较差的 CSS。
5. 使用 Tailwind 时，新增原子类后必须验证 H5 与目标小程序的实际表现。
6. 避免运行时大量动态拼接 class，尤其是任意值 class，防止小程序构建时样式被裁剪。
7. 组件样式优先放在当前 `.vue` 的 `<style lang="scss" scoped>` 中，减少全局污染。
8. 静态资源放入 `static/` 或项目既有静态目录，并使用目标端可打包的路径。
9. 跳转、存储、网络、支付和分享使用 `uni.*` 或项目已有封装，不直接依赖 Web API。
10. 涉及条件编译时分别检查各分支，修改后至少验证 H5 和实际目标端。

## 7. 小程序开发规范

- 使用目标平台提供的登录、授权、支付、分享、订阅消息和隐私能力，不自行模拟平台行为。
- 平台专属逻辑应使用条件编译或独立适配层，避免影响其他端。
- 页面生命周期、返回栈、分包、权限授权和前后台切换应按平台行为处理。
- 静态资源、组件和 API 必须符合目标平台的体积、域名和兼容性限制。
- 关键交互除开发工具外，还应在目标平台或真机环境验证。

## 8. App 开发规范

- 调用相机、相册、定位、通知、存储等系统能力前，应检查权限状态并处理拒绝、受限和再次授权场景。
- 正确处理启动、前后台切换、网络变化、页面恢复和应用被系统回收等生命周期情况。
- 本地存储不得保存明文密码、完整密钥或其他高敏感信息。
- 原生插件和平台专属能力应封装隔离，并分别验证 Android 与 iOS 行为。
- 外部链接、文件、深链和回调参数必须校验，不能直接信任其他应用传入的数据。

## 9. 安全编码规范

### 9.1 认证与密码

- 密码复杂度、失败锁定和会话时效使用项目统一策略，不在多个端分别硬编码。
- 密码必须使用成熟的强哈希算法存储，例如 `password_hash` 提供的算法；禁止新增 MD5、SHA-1 等快速哈希存储密码。
- 登录、找回密码、修改密码等接口应避免通过错误信息泄露账号是否存在。
- Token、Session 和刷新凭证应使用项目统一机制生成、校验、续期和撤销。
- 账号状态或权限发生变化后，相关凭证和缓存权限应及时失效。

### 9.2 敏感数据

- 敏感数据遵循最小采集、最小存储、最小返回和最小授权原则。
- 页面、接口、导出、日志、埋点和异常上报中的敏感数据应按统一规则脱敏。
- 需要还原的敏感数据使用成熟加密方案存储，密钥与数据分离管理；禁止自创加密算法或硬编码密钥。
- 密码、Token、身份证号、银行卡号等敏感值不得放入 URL 查询参数。
- 客户端只接收当前功能必需的数据，不得依赖前端隐藏实现数据保密。

### 9.3 请求安全

- 所有状态变更接口都必须执行身份、权限、参数和业务状态校验。
- 涉及签名、防重放或请求加密时，复用项目统一协议和中间件，不创建私有变体。
- 签名校验应覆盖必要参数，并结合时间戳、随机串或唯一请求标识控制有效期和重复使用。
- Web 项目应按框架规范处理 CSRF、XSS、SQL 注入、开放重定向和文件路径穿越。
- 动态 SQL、HTML、URL、命令和文件路径必须使用对应的安全 API 构造，禁止通过字符串拼接规避框架保护。

## 10. 文件与 Git 规范

- `.env`、运行时目录、IDE 配置、依赖目录、构建产物、上传文件和证书等内容应按项目情况加入忽略规则。
- 示例配置只能包含字段说明和无敏感性的示例值，不得包含真实环境信息。
- 未经明确要求，不执行提交、推送、合并、打标签、建分支或改写历史等 Git 操作。
- 不覆盖或撤销他人已有修改；发现非当前任务改动时，应保留并避开。
- 提交前检查变更范围，确保不包含调试文件、日志、缓存、生成文件和敏感信息。